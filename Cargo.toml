[workspace]
resolver = "2"
members = [
    "crates/utopia-core",
    "crates/utopia-store",
    "crates/utopia-server",
    "crates/utopia-ingest",
    "crates/utopia-extract",
    "crates/utopia-reason",
    "crates/utopia-search",
    "crates/utopia-llm",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/deeplethe/utopia"

[workspace.dependencies]
utopia-core = { path = "crates/utopia-core" }
utopia-store = { path = "crates/utopia-store" }

tokio = { version = "1", features = ["full"] }
axum = { version = "0.8", features = ["multipart"] }
axum-extra = { version = "0.10", features = ["cookie"] }
tower-http = { version = "0.6", features = ["cors", "trace", "fs", "set-header"] }
sqlx = { version = "0.8", default-features = false, features = [
    # mysql 只为问数引擎而开（query_engine::mysql）：一条线协议顺带覆盖
    # TiDB / OceanBase / Doris / StarRocks。本体的库始终是 postgres。
    # bigdecimal 是 DECIMAL 唯一的读法——驱动把 DECIMAL 同时挡在 f64 和 String
    # 之外（"floating-point numbers have different semantics"），没有它金额列会整列变 null
    "runtime-tokio", "tls-rustls", "postgres", "mysql", "bigdecimal", "uuid", "chrono", "json", "migrate", "macros",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
anyhow = "1"
uuid = { version = "1", features = ["v4", "v7", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
argon2 = { version = "0.5", features = ["std"] }
# 凭据静态加密（core::secrets）：AES-256-GCM，钥匙不进库
aes-gcm = "0.10"
base64 = "0.23"
# 密钥比对（sources 的推送密钥）：常量时间。已随 aes-gcm 进依赖树，显式列出
subtle = "2"
jsonwebtoken = { version = "10", features = ["rust_crypto"] }
figment = { version = "0.10", features = ["env"] }
dotenvy = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
flate2 = "1"
# 枚举的字符串化与遍历（来源种类 `SourceKind`）——手写两份清单的漂移就是 #247
strum = { version = "0.27", features = ["derive"] }

# Phase 1: 摄入/检索/LLM
utopia-ingest = { path = "crates/utopia-ingest" }
utopia-reason = { path = "crates/utopia-reason" }
utopia-search = { path = "crates/utopia-search" }
utopia-llm = { path = "crates/utopia-llm" }
tantivy = "0.26"
tantivy-jieba = "0.20"
text-splitter = { version = "0.32", features = ["tiktoken-rs"] }
# 分块预算按 token 数：cl100k 的排名表随 crate 内嵌，离线可用
tiktoken-rs = "0.12"
pdf-extract = "0.12"
calamine = "0.36"
pgvector = { version = "0.4", features = ["sqlx"] }
# `gzip` 不是可选项：一次嵌入回 64×1024 个浮点数，写成 JSON 文本每个数字占
# 21 字节而信息量只有 4 字节，一次响应 1.33 MB。实测同一条链路上不压缩 62 秒、
# gzip 21 秒（429 KB）——服务端算完只用 1 秒，其余全在传。
# `default-features = false` 把它一并关掉了，这里补回来。
reqwest = { version = "0.13", default-features = false, features = ["json", "form", "stream", "http2", "rustls", "webpki-roots", "charset", "system-proxy", "gzip"] }

feed-rs = "2"
cron = "0.12"
infer = "0.22"
sha2 = "0.11"
dom_smoothie = "0.16"
htmd = "0.5"
pulldown-cmark = { version = "0.13", default-features = false }
chardetng = "1"
encoding_rs = "0.8"
quick-xml = "0.42"
zip = { version = "6", default-features = false, features = ["deflate"] }
csv = "1"

# 本体导入：Oxigraph 拆出来的小而专的解析器，只解析不推理——
# 我们不需要 horned-owl 那套 OWL 推理机，投影只消费 RDF 三元组
oxttl = "0.2"
oxrdfxml = "0.2"
oxrdf = "0.3"
oxrdfio = "0.2"
async-stream = "0.3"
futures-util = "0.3"
tokio-stream = "0.1"
tempfile = "3"

[profile.dev]
debug = 1

[profile.release]
lto = "thin"
strip = true
